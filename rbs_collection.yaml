# Download sources
sources:
  - type: git
    name: ruby/gem_rbs_collection
    remote: https://github.com/ruby/gem_rbs_collection.git
    # Pinned to a specific revision (not `main`) so the resolved Nokogiri signatures
    # are reproducible: an unrelated upstream change can't silently flip type-check CI.
    revision: 5689759efdc7875d533e8e5b2376b4ab89472e36
    repo_dir: gems

# You can specify local directories as sources also.
# - type: local
#   path: path/to/your/local/repository

# A directory to install the downloaded RBSs
path: .gem_rbs_collection

# gems:
#   # If you want to avoid installing rbs files for gems, you can specify them here.
#   - name: GEM_NAME
#     ignore: true
