AllCops:
  TargetRubyVersion: 3.2
  NewCops: enable

# Don't fail for having too many tests
Metrics/BlockLength:
  AllowedMethods: ['describe']

Gemspec/RequireMFA:
  Enabled: false

plugins:
  - rubocop-rspec

require:
  - rubocop-rake
